{
  "name": "testredux",
  "version": "1.0.0",
  "private": true,
  "description": "Test",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify -t [ babelify --presets [ react ] ] src/app.js -o bundle.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "redux-thunk": "^1.0.0"
  }
}
